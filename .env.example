# Server Configuration
PORT=3000
SESSION_SECRET=change-this-to-a-random-secure-string
SESSION_MAX_AGE=604800000

# Optional: Encryption key for Google OAuth refresh tokens
# If not set, a default key will be used (acceptable for development)
# For production, generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=
